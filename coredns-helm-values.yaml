# helm repo add coredns https://coredns.github.io/helm
# helm pull coredns/coredns --version 1.40.0
# helm upgrade --namespace kube-system --install coredns ./coredns-1.40.0.tgz --namespace kube-system --values coredns-helm-values.yaml
service:
  clusterIP: fda6:3c52:d12b::10
serviceAccount:
  create: true
