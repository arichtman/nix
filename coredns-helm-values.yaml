# helm repo add coredns https://coredns.github.io/helm
# helm pull coredns/coredns --version 1.40.0
# helm upgrade --namespace kube-system --install coredns ./coredns-1.40.0.tgz --namespace kube-system --values coredns-helm-values.yaml --version 1.40.0
service:
  clusterIP: 2403:580a:e4b1:1:ffff:ffff:ffff:10
serviceAccount:
  create: true
